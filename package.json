{
  "name": "portfolioo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix my-app\" \"npm run server --prefix Backend\"",
    "build": "npm run build --prefix my-app",
    "start": "npm run start --prefix Backend",
    "test": "npm run test --prefix Backend && npm run test --prefix my-app"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
